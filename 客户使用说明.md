# FrameFlow 客户使用说明（Windows 完全版）

## 第一次打开

1. 将压缩包完整解压到普通文件夹，不要直接在压缩软件中运行。
2. 双击 `FrameFlow.exe`，等待浏览器自动打开。若 Windows 首次提示网络权限，仅允许专用网络即可。
3. 首次进入会显示“模型与 API 配置”：填写客户自己的 MiniMax、火山方舟或阿里百炼密钥，然后保存。

完全版已经内置 Python 运行环境、前端、FFmpeg、Real-ESRGAN、背景音乐、音效和示例素材，不需要另外安装 Python、Node.js、Ollama 或 FFmpeg。密钥只保存在本机 `backend/.env`，不会写入网页代码。

## 创作一条视频

1. 上传至少一张清晰商品图；人物图和场景图可选，但同时提供更利于保持镜头一致性。
2. 填写商品名称、真实尺寸和可核验卖点，选择时长、画幅、声音和音乐。
3. 让 AI 并行生成三个脚本方向，预览后锁定其中一个。
4. 点击生成，系统依次处理关键帧、视频镜头、旁白、字幕、音乐音效和最终剪辑。
5. 页面会显示当前阶段、供应商错误和实际费用；完成后可预览并下载 MP4。

## 常见问题

- **余额不足**：到相应供应商控制台充值后，返回项目恢复失败节点，不需要重做已完成内容。
- **浏览器没有打开**：手动访问 `http://127.0.0.1:8000`；若端口被占用，程序会在 8001–8010 中自动选择。
- **Failed to fetch**：确认 `FrameFlow.exe` 仍在运行，并且浏览器地址与程序实际端口一致。
- **更换密钥**：点击右上角设置，在配置页保存新密钥。
- **关闭程序**：关闭 `FrameFlow.exe` 进程即可；仅关闭浏览器不会停止后台任务。

请勿把已经填写密钥的整个文件夹转发给第三方。需要再次交付时，应清空 `backend/.env` 中的密钥。
