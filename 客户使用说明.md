# FrameFlow 客户使用说明

## 第一次打开

1. 在电脑上安装 Python 3.12+、Node.js 20+ 和 FFmpeg，并确保安装程序已将它们加入 PATH。
2. 双击项目根目录中的 `启动FrameFlow.bat`。第一次启动会自动安装项目依赖，可能需要几分钟。
3. 浏览器会自动打开 `http://127.0.0.1:8000`。首次进入时填写百炼 API Key，并选择 MiniMax 海螺或火山引擎 Seedance，填写对应的视频 API Key。

API Key 只保存在本机 `backend/.env`，不会返回给浏览器，也不会出现在前端代码中。以后再次打开无需重复填写。

## 一键生成视频

1. 上传至少一张清晰的商品图片；人物、场景和 Logo 图片可选，但建议同时上传以提高跨镜一致性。
2. 填写商品名称、真实卖点和期望故事，选择时长与画幅。
3. 设置本次最高预算，点击“生成完整视频”。系统会依次完成素材分析、故事脚本、关键帧、视频镜头、视觉质检、旁白、字幕、音乐音效和最终剪辑。
4. 页面会持续显示当前阶段、进度和实际费用。完成后直接预览并下载成片。

如果云端质检发现商品或人物明显变化，系统会停止继续付费，并在同一页面显示失败阶段与原因；它不会无限自动重试。

## 日常使用

以后只需双击 `启动FrameFlow.bat`。浏览器未自动打开时，手动访问 `http://127.0.0.1:8000`。

不要把已经填写 `backend/.env` 的文件夹直接转发给其他客户。交付新客户前，应删除 `backend/.env`，仅保留 `backend/.env.example`，让客户在首次配置页面填写自己的 Key。
