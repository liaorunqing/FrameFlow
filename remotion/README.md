# FrameFlow Remotion 包装层

该工程只负责数据驱动的 Logo、标题、CTA 和可复用动画包装。生成镜头、旁白、字幕与最终稳定交付仍由后端工作流和 FFmpeg 管理。

```powershell
npm install
npm run studio
```

商业使用前请核对 Remotion 许可：个人、不超过 3 人的营利组织、非营利组织及评估用途可免费使用；其他营利组织需要公司许可。
